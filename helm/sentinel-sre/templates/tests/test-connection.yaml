apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "sentinel-sre.fullname" . }}-test-connection"
  labels:
    {{- include "sentinel-sre.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "sentinel-sre.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
