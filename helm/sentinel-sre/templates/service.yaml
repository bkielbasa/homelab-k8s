apiVersion: v1
kind: Service
metadata:
  name: {{ include "sentinel-sre.fullname" . }}
  labels:
    {{- include "sentinel-sre.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sentinel-sre.selectorLabels" . | nindent 4 }}
